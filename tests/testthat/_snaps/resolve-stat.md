# resolve_stat() errors on an invalid stat_option

    Code
      resolve_stat("nope", NULL, NULL)
    Condition
      Error in `resolve_stat()`:
      ! Invalid stat_option. Choose 'single', 'both', or 'custom'.

