% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosstab.R
\name{crosstab}
\alias{crosstab}
\title{Create cross-tabulation summary tables}
\usage{
crosstab(
  data,
  main_vars,
  sub_vars,
  weight = NULL,
  strata = NULL,
  ids = NULL,
  fpc = NULL,
  stat_option = "both",
  custom_stat_cat = NULL,
  custom_stat_cont = NULL,
  main_digits = 0,
  sub_digits = 0,
  multiselect_stem = "r"
)
}
\arguments{
\item{data}{A data frame.}

\item{main_vars}{A character vector of variable names to include in the main table.}

\item{sub_vars}{A character vector of variable names to create subtables for.}

\item{weight}{Optional. Name of a numeric weight column in \code{data}. When
supplied, \code{crosstab()} builds a weighted survey design internally
(via \code{srvyr::as_survey_design()}) and uses \code{gtsummary::tbl_svysummary()}.
When \code{NULL} (the default), \code{data} is summarized directly with
\code{gtsummary::tbl_summary()}.}

\item{strata}{Optional. Name of a stratification column, used only when \code{weight} is supplied.}

\item{ids}{Optional. Name of a cluster/PSU column, used only when \code{weight} is supplied.}

\item{fpc}{Optional. Name of a finite population correction column, used only when \code{weight} is supplied.}

\item{stat_option}{String specifying the statistics to display: "single", "both", or "custom". Default is "both". "single" shows percentage for categorical variables and mean for continuous variables. "both" shows percentage and n for categorical variables and mean and standard deviation for continuous variables. "custom" uses \code{custom_stat_cat}/\code{custom_stat_cont}.}

\item{custom_stat_cat}{Only used when \code{stat_option = "custom"}. Follows the "statistic" argument conventions in gtsummary's \code{tbl_summary()}/\code{tbl_svysummary()}.}

\item{custom_stat_cont}{Only used when \code{stat_option = "custom"}. Follows the "statistic" argument conventions in gtsummary's \code{tbl_summary()}/\code{tbl_svysummary()}.}

\item{main_digits}{Number of digits for primary statistics.}

\item{sub_digits}{Number of digits for secondary statistics.}

\item{multiselect_stem}{Trailing stem preceding the digits in multiselect variable names (e.g. \code{"r"} for \code{M_TOPIC_r1}).}
}
\value{
A gtsummary object.
}
\description{
Creates a main summary table and one subtable per \code{sub_vars} entry, and
combines them into a single formatted cross-tabulation. Works on a plain
data frame by default; supplying \code{weight} (and optionally \code{strata}/\code{ids}/
\code{fpc}) switches to a weighted survey design internally, so there is no need
to pre-build an \code{srvyr} object.
}
\details{
Variables in \code{main_vars} whose names start with \code{"M_"} are treated as
multiselect groups (Y2 convention): variables sharing a prefix before a
trailing \code{multiselect_stem} + digits (e.g. \code{M_TOPIC_r1}, \code{M_TOPIC_r2}) are
collapsed into a single block, dropping "NO TO ..." and missing rows and
sorting the remaining levels by descending percentage.
}
\examples{
\dontrun{
  library(SurveySuite)

  # Unweighted
  crosstab(CES24_sample, main_vars = c("CC24_301", "CC24_302"), sub_vars = c("gender4", "educ"))

  # Weighted
  crosstab(
    CES24_sample,
    main_vars = c("CC24_301", "CC24_302"),
    sub_vars = c("gender4", "educ"),
    weight = "commonweight"
  )
}
}
